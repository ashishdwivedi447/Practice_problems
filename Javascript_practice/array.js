
// console.log(typeof([])===typeof({}));   // true
 // array is an object in Javascript thast's why it yields true .

 // object -===>

//  var name1=5;   it can't be 1name means variable declaration first letter can't be Number.

//  console.log(name1);

// let obj={
//     1:"ashish"
// }

// console.log(obj[1]);   ---> ashish but in case of number as key if we try to fetch a particular key's corresponding value then with dot notation we can's access that. like this obj.1 can't do like this.

// Types of ways to declare to an arrays ---->

// 1.Using square brackets and comma-separated values:
//    let arr=[1,2,3,45,50];
// 2. the array constructor with the new keyword.
// The Array constructor can also be used with a single argument to define the length of an empty array, like new Array(5).
//    let name=new Array(1,2,3,45,50);
 
//  let prince=new Array(4).fill(23);
//  console.log(prince);

 // 3. Using the Array.from() method:
//     const myArray = Array.from([1, 2, 3, 4, 5]);
//    console.log(myArray);




